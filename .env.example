# Base URL of the Limitra backend API. Includes the version segment — confirm
# against the live spec at <api-host>/docs?api-docs.json (servers[0].url).
# All client-side calls go through lib/api/client.js, which reads this.
NEXT_PUBLIC_API_URL=https://api.limitra.com.ng/api/v1
